spring.application.name=hackathon
server.port=8086

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/hackathon}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:argusadmin}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Redis Configuration
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}

# Security Configuration
spring.security.user.name=user
spring.security.user.password=password

# OAuth2 Google
spring.security.oauth2.client.registration.google.client-id=<GET_IT_FROM_DOC>
spring.security.oauth2.client.registration.google.client-secret=<GET_IT_FROM_DOC>
spring.security.oauth2.client.registration.google.scope=profile,email

#JWT
application.security.jwt.secret-key=<GET_IT_FROM_DOC>

application.security.jwt.expiration=86400000