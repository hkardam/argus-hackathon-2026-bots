# Frontend Formatting Rule

**Do not perform manual formatting.**
All code formatting must be handled using the project's automated tools:

- Run `npm run format` to format the entire project.
- Run `npm run lint --fix` to fix both linting and formatting issues.
- Ensure VS Code "Format on Save" is enabled.
